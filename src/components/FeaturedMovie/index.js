import React from "react";

import "./style.css"

export default ({item}) => {
    return (
        <section className="featured">
            <div>
                
            </div>
        </section>
    )
}