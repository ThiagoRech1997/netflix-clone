import React from "react"
import { Link, useHistory } from "react-router-dom"

export default function Login(){
    return(
        <div>
            <Link to="/watch">
                <button>Hello</button>
            </Link>
        </div>
    )
}